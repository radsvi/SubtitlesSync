<Window x:Class="SubtitlesSync.View.Windows.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SubtitlesSync.View.Windows"
        mc:Ignorable="d"
        WindowStyle="SingleBorderWindow"
        Background="#F0F0F0"
        Title="OptionsWindow" Height="400" Width="800">
    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{Binding CloseOptionsWindowCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <StackPanel>
            <GroupBox Header="Other">
                <DockPanel>
                    <Label Content="Subtitles source"/>
                    <TextBox x:Name="tbSubtitlesSource" d:Text="https://www.opensubtitles.org/en/search2/sublanguageid-eng/moviename-%s" Text="{Binding SubtitlesDownloadSource}"
                             VerticalAlignment="Center" Height="24" VerticalContentAlignment="Center" />
                </DockPanel>
            </GroupBox>
            
            <GroupBox Header="Registry">
                <StackPanel>
                    <Button Content="Create &quot;Search for subtitles&quot; context menu"
                            Command="{Binding SearchContextMenuCommand}"/>
                    <Button Content="Create &quot;Sync subtitles with video&quot; context menu"
                            Command="{Binding SubtitlesSyncContextMenuCommand}"/>
                    <Button Content="Remove context menus"
                            Command="{Binding RemoveContextMenuCommand}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>


    </Grid>
</Window>
